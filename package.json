{
  "name": "alpha-beta-charlie",
  "version": "0.0.1",
  "homepage": "https://brendansudol.github.io/alpha-bravo-charlie",
  "private": true,
  "dependencies": {
    "ace-css": "^1.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
